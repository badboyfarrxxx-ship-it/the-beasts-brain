---
status: active
project: personal
type: log
created: 2026-08-28
---
<!-- Every daily note gets created from a copy of the Daily Note Template. -->

# Friday, August 28, 2026

## Index

- **Built The Beast (full stack install)** — memory, voice, face, and hands all installed, wired together, and tested end to end.

## Session 1 — afternoon: Full stack install

### What Got Done
- Installed prerequisites: git, Obsidian, uv, Python 3.12, eSpeak NG, Microsoft C++ Build Tools, Google Chrome (all via winget).
- **Memory:** created the Obsidian vault "The Beasts Brain" at `C:\Users\badbo\The Beasts Brain`, registered it in `obsidian.json`, built the folder structure (Inbox, Daily Notes, Personal, Archive, Resources) with indexes, `VAULT-INDEX.md`, `Active Priorities.md`, and the daily note template. Wrote `CLAUDE.md` (The Beast's boot config) in `C:\Users\badbo\my-agent`.
- **Voice (backtalk):** set up the Python venv, downloaded the Whisper and Kokoro speech models, configured push-to-talk on the backtick key, built-in `bm_lewis` voice, permission mode "ask". Confirmed audio output and a full spoken round-trip.
- **Face (ai-visualizer):** radial face, port moved to 8600 (default 8790 is inside a Windows reserved port range). Wired to backtalk's signal bus.
- **Hands (barehands):** port moved to 8601 (same reason). Orbs: the vault ("Brain"), the sample notes, and the media airlock. Wired the ring to the voice via `barehands_state_dir`, and added a project-scoped hook in `.claude/settings.json` so the ring also reacts during typed sessions.
- **Marketing:** installed Jared Rhodenizer's marketing playbook into the vault at `04 - Resources/Marketing/` with an index note that primes The Beast to read the principles before any marketing work.
- Created four Desktop shortcuts: Chat with The Beast, Talk to The Beast, The Beast barehands, Update The Beast.
- First hello: launched "Talk to The Beast", the greeting spoke, and Nathan held the key and got a spoken reply.

### What's Still In Progress
- Vault has no backup yet (lives only on this disk). Options noted for when Nathan wants them: Windows File History or a private GitHub repo.
- The backtick push-to-talk key is also a key Nathan types with — flagged that we can switch it (F13 / right-ctrl) if it misfires.

### Decisions Made
- Agent identity built from scratch (door C): name "The Beast", personality funny / rude / warm./cheeky/cheesy
- Voice engine: built-in (offline), not ElevenLabs.
- Face: radial. Mic: push to talk. Permissions: ask first.
- Ports 8600 (face) and 8601 (hands) chosen to avoid the Windows reserved ranges shown by `netsh interface ipv4 show excludedportrange`.
- `webrtcvad` compiled from source after installing MS C++ Build Tools, rather than shipping a workaround — keeps the stock launcher scripts working and gives Nathan a compiler for his own program-building.

### Notes Touched
- [[VAULT-INDEX]] — created, then updated the structure map for the Marketing folder.
- [[Active Priorities]] — seeded with "get this computer set up to build programs" and "start making money from the programs I build".
- [[Marketing]] — created as the marketing playbook index.
- `04 - Resources` index — added the Marketing entry.

### Profile Updates
- **Key People:** added [[Margaret McMahon]] (Nathan's partner).
