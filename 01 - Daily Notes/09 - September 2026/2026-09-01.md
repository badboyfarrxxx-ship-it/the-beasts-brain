---
status: active
project: personal
type: log
created: 2026-09-01
---
<!-- Every daily note gets created from a copy of the Daily Note Template. -->

# Tuesday, September 1, 2026

## Index

- **Freed space on C: and moved the VLC dev build back to the SSD.** Cleared ~4.5 GB on C: (Temp, pip cache, hibernation off), then moved `Builds\VLC` from the G: HDD back to `C:\Users\badbo\Builds\VLC`. GUI verified, shortcut and plugin cache fixed.
- **Booted as The Beast; full stack health check.** Moved the session into `my-agent` off the generic starter template, ran the real startup sequence, and verified all four tools (vault, face, board, voice line) run. Voice line booted clean, Nathan confirmed audio.

## Session 1, 8:00 AM: VLC back to C:, C: cleanup

### What Got Done
- **Cleared space on C:** (it was at 7.5 GB free, not the ~21 GB the Aug 29 note ended at):
  - `AppData\Local\Temp` cleared — ~1.2 GB (kept the live Claude scratchpad; 9 locked files skipped).
  - `pip cache purge` — 62 MB.
  - Recycle Bin — already empty.
  - **Hibernation disabled** (`powercfg /h off`, run elevated via a UAC prompt) — removed `hiberfil.sys`, ~3.1 GB. Fast Startup is off as a side effect; not missed on this machine.
  - **Net: C: 7.5 GB -> 12.0 GB free.**
- **Moved the runnable VLC build back to the SSD:** `G:\Builds\VLC` -> `C:\Users\badbo\Builds\VLC` (robocopy `/MOVE`, 1935 files, 1.51 GB, 0 failures). Reason: G: is a spinning HDD, so plugin scans and launch dragged.
- Regenerated `plugins\plugins.dat` at the new path with `vlc-cache-gen.exe` (the old cache stores absolute plugin paths; a stale one drops VLC to a windowless CLI).
- Re-pointed the Desktop shortcut **"VLC 4.0 (dev build)"** -> `C:\Users\badbo\Builds\VLC\vlc.exe` (workdir + icon updated).
- **Verified:** `vlc.exe` launches the full Qt QML GUI from C: — 200 MB working set, window titled "VLC media player", no errors.
- **C: after the move: ~10.5 GB free.**

### What's Still In Progress
- **uv package cache (~1.6 GB) not cleared** — the cache is locked by a running `browser-use` MCP server (`uvx browser-use --cli-mcp`, PID 10840 + 2 python workers), one of which executes from inside the cache. Clear it later when that MCP isn't running; not worth killing an active tool over.
- Source + build tree stays at `G:\vlc` (6.8 GB); distributable installer stays at `G:\Builds\installer\` (306 MB). Neither needs the SSD.

### Decisions Made
- **Runnable VLC lives on C: (SSD); source and installer stay on G: (HDD).** Rebuilds are rare and slow regardless of drive; the thing that benefits from the SSD is day-to-day launch and plugin scanning.
- **Hibernation stays off** — the space is worth more than hibernate/Fast Startup on this box.

### Notes Touched
- [[Building VLC for Windows]] — updated the Status and drive-layout bullets: runnable build path `G:` -> `C:\Users\badbo\Builds\VLC`, installer path corrected to `G:\Builds\installer\`, added the "moving the runnable folder needs cache-gen + shortcut re-point" note and the reason for the move.
- [[01 - Daily Notes]] index — added this entry.

### Profile Updates
- (none)

## Session 2, 11:25 PM: booted as The Beast, full stack health check

### What Got Done
- **Boot.** Session opened with no folder set, and Nathan first pasted the *generic* starter boot template (identity "Jarvis", unfilled `[FILL IN]` markers). Flagged that his real config already exists and is complete. On his OK, moved the session into `C:\Users\badbo\my-agent` and ran the real startup sequence (VAULT-INDEX, recent daily notes, Active Priorities). Aug 30-31 have no notes and no recoverable context, so they are treated as days off.
- **Stack health check ("set you up properly"), 4/4 green:**
  - *Vault*: intact. Obsidian has it registered at `The Beasts Brain`, marked open.
  - *Face (ai-visualizer)*: started `server.py --mock`, serving on `:8600` (radial).
  - *Board (barehands)*: started `server.py`, `:8601` state API live, board empty.
  - *Voice (backtalk)*: venv complete (torch 2.13.0, kokoro 0.9.4, faster-whisper 1.2.1, openwakeword 0.6.0, spaCy + `en_core_web_sm`, `claude_agent_sdk`). `backtalk.json` valid (`bm_fable` / `tts_device` 8 / `base.en` / wake phrase "hey beast").
- **Voice line run for real.** Closed Firefox first (holding ~1.7 GB across 18 procs; RAM was down to 1.0 GB free). `run.sh` booted the full line clean in ~90 s: brain warm, faster-whisper STT ready, kokoro "voice ready", wake gate armed, no `mkl_malloc`. Nathan confirmed it ("yes"). With the full stack up (this session plus voice line, face, board) the box sits at **~0.7 GB free of 7.8**, redlined.

### What's Still In Progress
- **Vault backup, not started.** Neither the vault nor `my-agent` is under git; single disk, no copy. Flagged since Aug 28. Method being decided: private GitHub repo (`gh` not installed) vs OneDrive (present, running) vs local mirror to D: (1 TB free).
- **`CLAUDE.md` "Make it yours" section**: still the shipped stub; Nathan to dictate his own hard lines.
- **The three tool servers** (face, board, voice line) are running as this session's background tasks only, and need a proper persistent launch (Desktop shortcuts / autostart).
- **`G:` anomaly**: `Get-PSDrive` showed `G:` as 0 B / 0 B this session. The VLC source tree (`G:\vlc`, carrying the two local source patches) may be on a drive that is currently offline. Unverified, check next session.

### Decisions Made
- Booted as **The Beast** from the real `my-agent\CLAUDE.md`, not the generic starter template Nathan first pasted.

### Notes Touched
- [[01 - Daily Notes]] index: extended the 2026-09-01 line.

### Profile Updates
- (none)
