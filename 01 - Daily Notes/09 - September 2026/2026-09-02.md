---
status: active
project: personal
type: log
created: 2026-09-02
---
<!-- Every daily note gets created from a copy of the Daily Note Template. -->

# Wednesday, September 2, 2026

## Index

- **Vault backup to private GitHub (setup)** — chose the GitHub option, installed `gh`, built and committed both local repos (`the-beasts-brain`, `the-beast-agent`); push is pending a one-time `gh auth login` from Nathan.

## Session 1 — 12:05 AM: vault backup to private GitHub

### What Got Done
- Nathan picked **private GitHub repo** for backup, over OneDrive and a local D: mirror.
- Installed **GitHub CLI** — `winget install GitHub.cli`, `gh` 2.98.0 at `C:\Program Files\GitHub CLI\gh.exe`.
- Set global git identity: `Nathan` / `badboyfarrxxx@gmail.com`.
- **Vault repo** — `git init` in `C:\Users\badbo\The Beasts Brain`; `.gitignore` covers Obsidian `workspace.json` + caches + OS cruft; initial commit `81f8937`, 30 files.
- **Agent repo** — `git init` in `C:\Users\badbo\my-agent`. The six tool folders are upstream jaredrhod clones with their own `.git`, so git can't track files inside them — the three live tool configs are copied into a tracked `tool-configs/` folder instead. `.gitignore` excludes the tool dirs + Claude Code runtime cruft. Tracks `CLAUDE.md`, `.claude/settings.json`, `.gitignore`, `tool-configs/` (backtalk / ai-visualizer / barehands json + a README). Initial commit `94e880e`.
- Wrote it all up: [[Vault backup]] in `04 - Resources`.

### What's Still In Progress
- **Neither repo is pushed.** Both are local-only until Nathan runs `gh auth login` (interactive browser flow — the AI can't run it or handle the credentials). Then: `gh repo create the-beasts-brain --private --source=... --push` and the same for `the-beast-agent`. Commands are in [[Vault backup]].
- **Commit cadence** not set — The Beast commits at each checkpoint, or a scheduled task auto-commits daily. Undecided.
- **backtalk source patches** (`mouth.py` / `config.py` / `ears.py`) still not version-controlled — documented in [[backtalk voice - operating notes]] and flagged in `tool-configs/README.md`. Separate task.
- **`CLAUDE.md` "Make it yours"** section — still the shipped stub; pending Nathan's own hard lines.
- **Three tool servers** (face `:8600`, board `:8601`, voice line) still running only as this session's background tasks — no persistent launch yet.
- **`G:` anomaly** from last session — still unverified (`Get-PSDrive` showed it as 0 B; the VLC source tree may be on an offline drive).

### Decisions Made
- Backup method: **private GitHub, two repos** — `the-beasts-brain` (vault) and `the-beast-agent` (`my-agent` config). Over OneDrive (sync-conflict risk) and a local D: mirror (no off-site protection).
- The agent repo tracks config only; the tool clones restore from upstream, and their configs live as refreshed copies in `tool-configs/`.

### Notes Touched
- [[Vault backup]] — created.
- `04 - Resources` index — added the [[Vault backup]] entry.
- [[01 - Daily Notes]] index — added 2026-09-02.

### Profile Updates
- (none)
