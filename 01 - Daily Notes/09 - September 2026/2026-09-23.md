---
status: active
project: personal
type: log
created: 2026-09-23
---

# Wednesday, September 23, 2026

## Index

- **Undermoot demo designed:** with Nathan's three decisions (end after zone 4, carry progress over by save code, cut at build time), the free demo build is specced and awaiting his review; the multi-agent brainstorm he asked for couldn't run because PAL isn't installed.
- **Undermoot demo built:** all six plan tasks done test-first on the `demo-build` branch; final review in progress, then merge and delivery.

## Session 1, 12:40 AM: Undermoot demo design (carried over from the 09-22 session)

### What Got Done
- Designed the free demo build of Undermoot through the brainstorming process (classed as architectural: questions one at a time, then a two-part design, then a written spec).
- Worked out the cut-off from the game's own progression simulation: zones 1 to 3 are 52 of 290 fights (level 13), zones 1 to 4 are 82 (level 18, which includes "The Choosing of the Brood").
- Nathan invoked `clink-brainstorm`. It could not run: the PAL MCP server it needs is not installed (not in `claude mcp list`, no copy on disk), and of its agents only the Codex CLI is on the machine. Continued the design directly with Nathan. Setting PAL up was not proposed; that is Nathan's call.
- Spec written and committed in the working copy: `Documents/colony-game/docs/superpowers/specs/2026-09-23-demo-build-design.md` (commit `618e4aa`). [[Selling Undermoot]] Step 1 updated with the decisions.

### What's Still In Progress
- Nathan to review the written spec; then an implementation plan, then the build (test-first).

### Decisions Made
- The demo ends after zone 4, Bramble Sinks, right after the Choosing of the Brood (Nathan).
- Demo progress carries into the full game through the existing save code (Nathan).
- The demo is cut from the sale content at build time, so the paid zones aren't in the free file (Nathan).

### Notes Touched
- [[Selling Undermoot]].

### Profile Updates
- None.

## Session 2, 1:15 AM: Undermoot demo built

### What Got Done
- Nathan approved the spec, then the implementation plan (`docs/superpowers/plans/2026-09-23-demo-build.md`, commit `df49f46`), and picked inline ("Native") execution.
- Built all six plan tasks test-first on a local branch `demo-build` in `Documents/colony-game` (7 commits, `dc79585` to `0f2d8bf`): the content cutter (`src/demo.js`, 8 tests), a save guard so a full-game code pasted into the demo doesn't crash it (`src/saveguard.js`, 5 tests), the third build (`GAME_PACK=demo` -> `dist/undermoot-demo.html`), a leak check that the paid content isn't in the demo file, the end screen with the save code, and the README.
- Checks: 22 unit tests pass; progression passes for all three packs (demo: 82 fights, level 18); the name and leak scans are clean on both builds, and the leak check fails on the full game as it should; seven browser checks in the Claude pane all pass, plus a real click on Copy.
- Found and fixed a mistake in the spec and plan: zone 5 is **The Long Dig**, not Moonlit Surface (that's zone 6). The code was right; the docs were corrected.

### What's Still In Progress
- A fresh reviewer is going over the whole branch. After that: fixes if any, merge to `master` (Nathan's OK), and delivery of the three HTML files and the source zip to the vault.

### Decisions Made
- Work on a branch rather than straight on `master`, so the change can be reviewed as one piece.

### Notes Touched
- None yet (the delivery updates [[colony-game]] and [[Selling Undermoot]]).

### Profile Updates
- None.

## Session 3, 1:45 AM: colony-game pause fix, merge and GitHub backup

### What Got Done
- Fixed the root cause of the hidden 3D world waking up after colony-screen battles: the world now tracks whether it is open, and `onBattleResolved()` only unpauses an open world (commit `9ff3b7f`). Verified in the browser pane; all three packs built; unit, progression and scan pass.
- Fast-forwarded `demo-build` into `master` (Nathan's call), no conflicts.
- Created the private GitHub repo `badboyfarrxxx-ship-it/colony-game` (Nathan's OK), pushed `master` and `demo-build`, set the default branch to `master`.

### What's Still In Progress
- `test/smoke.cjs` still loads Playwright from a Linux path and can't run on this machine (needs Nathan's OK, it's a test source edit).
- Delivery of the three HTML files and the source zip into the vault folder is still pending from Session 2.

### Decisions Made
- Merge by fast-forward rather than cherry-picking the fix onto `master` (Nathan).
- Back up colony-game to a private GitHub repo (Nathan).

### Notes Touched
- [[colony-game]].

### Profile Updates
- None.
