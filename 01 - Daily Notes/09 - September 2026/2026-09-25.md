---
status: active
project: personal
type: log
created: 2026-09-25
---

# Friday, September 25, 2026

## Index

- **VLC crash and rebuild plan:** gave crash checks for the installed VLC 4.0 dev build, then agreed a new build process with Nathan and wrote the spec.
- **VLC build plan:** Nathan approved the spec; wrote the implementation plan and tested its scripts (32 tests pass).
- **VLC build, first pass:** built and reviewed Tasks 1-5 in a local `vlc-build` repo; waiting on Nathan to create the GitHub repo so it can be pushed and built.

## Session 1, 6:40 PM: VLC crash and rebuild plan

### What Got Done
- Nathan reported that his VLC dev build crashes as soon as it opens. From a cloud session I couldn't check the Surface, so I gave him checks to run: the Windows crash log (faulting module), forcing Qt's graphics backend (a missing Intel graphics driver on the fresh tiny11 install is the top suspect), resetting `%APPDATA%\vlc`, and regenerating the plugin cache. Results not back yet.
- Brainstormed a rebuild with upgrades. Split into four sub-projects: build pipeline, swipe to seek, camera wall in VLC, video editing.
- Wrote the spec for sub-project 1: [[VLC build pipeline]]. VLC 4 gets built on GitHub's build servers using VLC's own build image and scripts, and comes out as a portable zip. Nothing is installed on the Surface.

### What's Still In Progress
- Nathan to review the spec. Then an implementation plan, then create the `vlc-build` repo and run the first build.
- The crash cause on the old build is still unknown.

### Decisions Made
- Build on GitHub Actions (option A), not in a cloud session and not on the Surface. Reason: repeatable, and the Surface stays a media box.
- Output is a portable zip, not an installer, so it can't clash with a normal VLC.
- Nathan wants shorter explanations for this build work.

### Notes Touched
- [[VLC build pipeline]] (new), [[04 - Resources]], [[Active Priorities]].

### Profile Updates
-

## Session 2, 6:50 PM: VLC build plan

### What Got Done
- Nathan approved [[VLC build pipeline]]. Wrote [[VLC build pipeline - implementation plan]]: six tasks, from creating the `vlc-build` repo to Nathan's test on the Surface.
- Checked the build details against VLC's current source (master `9e59d4b`): the build image name, `build.sh` flags, and the `make package-win32-zip` target and its output layout.
- Ran the plan's four scripts and their tests in a scratch folder: 32 tests pass.

### What's Still In Progress
- Nathan to review the plan and pick how it gets carried out. Also to confirm commits go straight to `main` in the new repo.

### Decisions Made
- Builds are published as GitHub releases, not workflow artifacts. Artifacts come double-zipped and expire; a release is one plain download that stays.
- This sandbox can't reach `videolan.org`, so the real build can only be tested on GitHub's servers.

### Notes Touched
- [[VLC build pipeline - implementation plan]] (new), [[VLC build pipeline]], [[04 - Resources]], [[Active Priorities]].

### Profile Updates
-

## Session 3, 7:20 PM: VLC build, first pass

### What Got Done
- Carried out the plan task by task, with a helper writing each part and a separate reviewer checking it. Tasks 1-4 (repo skeleton and four tested scripts) and the build workflow from Task 5 are committed in a local repo, 32 tests passing.
- Review of the build workflow caught two bugs in the plan's own code: a failure to read VLC's build image would have been hidden, and VLC's `build.sh -r` already makes the zip, so the extra zip step was dead. Both fixed, re-reviewed, and the plan updated to match.

### What's Still In Progress
- **Blocked on Nathan:** creating the repo from this session is refused (GitHub 403). Nathan to create `vlc-build` (private, empty) and give the Claude GitHub app access. Then: push, first build on GitHub, his test on the Surface.

### Decisions Made
- Commits go straight to `main` in `vlc-build` (Nathan, this session).
- Keep `build.sh -r`: it also builds a .7z and an installer that aren't published. Costs a few minutes per build.

### Notes Touched
- [[VLC build pipeline - implementation plan]], [[VLC build pipeline]].

### Profile Updates
-
