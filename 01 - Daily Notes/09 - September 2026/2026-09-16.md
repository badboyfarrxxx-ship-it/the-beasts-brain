---
status: active
project: personal
type: log
created: 2026-09-16
---

# Wednesday, September 16, 2026

## Index

- **Restore from H: finished:** WSL was the last blocker and came back after DISM-enabling the
  virtualization features and rebooting. Post-install hardening is still untouched.
- **GitHub push:** the vault was 12 commits ahead, not 7. With the Claude shell dead again, Nathan
  installed Git, signed in and pushed both repos himself; verified at `b7e4f45` and `fd8e579`.

## Session 1: carried over from the 15th

### What Got Done

- Opened this note. The rebuild session ran continuously from the 15th, so the work itself
  (restore verification, the `.claude` folder renames and `cwd` rewrite, the rescued memory
  files, and the `E:` File History find) is written up under Session 3 of [[2026-09-15]]
  rather than repeated here.
- **WSL is back.** The `HCS_E_SERVICE_NOT_AVAILABLE` failure was tiny11 shipping Virtual
  Machine Platform disabled, with `wsl --install` only staging it. Nathan DISM-enabled
  `VirtualMachinePlatform` and `Microsoft-Windows-Subsystem-Linux`, rebooted, and the
  `wsl --import-in-place` of the Ubuntu disk then worked. The vhdx sits on local storage at
  `C:\Users\Fredy 2\WSL\Ubuntu\ext4.vhdx`, not on the Seagate, which is what protects it from
  corruption when the drive sleeps.
- **The restore from H: is now complete and verified.** Checked on disk this session: the vault
  (421 files), `my-agent` (all six repos), the `.claude` history under corrected project paths,
  Pictures (all 17 files matching the backup) and `7+.vhd` in `Documents\restored-misc\`. The
  only item not machine-verifiable is the two Edge bookmarks, which are a manual re-add.
- **Repaired the stale `badbo` paths in the live config**, found while preparing to hand over to
  a fresh session. All of these would have broken or silently misdirected it:
  `my-agent\CLAUDE.md` (4 paths, including the vault location on line 3),
  `my-agent\.claude\settings.json` (both barehands state hooks), the three
  `my-agent\tool-configs\*.json` copies (6 paths), and the vault location in `VAULT-INDEX.md`.
- **Left the historical `badbo` paths alone on purpose.** They appear throughout the older daily
  notes and in [[Building VLC for Windows]], [[Machine drives]] and [[Long-running jobs]],
  where they record what was true at the time. Rewriting those would falsify the record. The
  `VAULT-INDEX.md` fix carries a short note saying so.
- Flagged as stale rather than rewritten: the "Setup notes specific to this machine" block in
  `CLAUDE.md` now carries a dated warning. Verified absent on the rebuild: Python, `uv`, the
  backtalk `.venv` (excluded from the backup by design, so it must be recreated rather than
  restored), the C++ Build Tools, and Obsidian.

### What's Still In Progress

**Tooling to reinstall before the agent stack runs again**

- Python 3.12, `uv`, and the C++ Build Tools (`webrtcvad` needs them to compile).
- Recreate the backtalk `.venv`; it was never backed up. kokoro needs `pip` and the
  `en_core_web_sm` spaCy model in that venv, and a bare `uv sync` (exact) prunes them, so use
  `uv sync --inexact` as `start.bat` does.
- Obsidian, then re-register the vault so it opens "The Beasts Brain".
- Delete the stale-block warning in `CLAUDE.md` once the above is true again.

**Restore leftovers**

- Edge bookmarks: the two URLs were handed over to re-add by hand (the Alzex Finance user guide
  and the Freeview Australia TV guide). Last outstanding piece of the restore.
- No `MEMORY.md` index in the `my-agent` memory folder, which now holds six memories (the sixth,
  `windows-account-renamed-badbo-to-fredy-2`, was added at the end of Session 1). The
  existing files use the desktop app's frontmatter schema rather than the file-based one, so
  the index is on hold until we know which the stack actually reads.

**Post-install hardening, none of it started**

- BitLocker on (tiny11 disabled automatic encryption), recovery key to the Microsoft account or
  printed, never onto the Surface itself.
- Secure Boot back to "Microsoft Only" in the UEFI, now the install stick has done its job.
- Activation check: the image is Pro and the Surface's built-in key is Home, so it may report
  not activated.

**Security incident, open items**

- Vault is **7 commits ahead of GitHub**. Needs Nathan to sign in himself; the old token was
  revoked on 09-12. *(Corrected in Session 2: it is 12.)*
- `D:` (the SanDisk case) is plugged in and still unchecked, against step 17 of the reinstall
  plan. Nathan asked to unplug it and `E:`; that is physical, so it is his to do.
- **Correction: `E:` cannot be unplugged on its own.** Verified on the rebuild (a direct read of
  two known files) that `E:` is still the File History partition. Per [[Machine drives]], that
  partition sits on the 4 TB Seagate alongside `F:` and `H:`, the backup, all on one USB cable.
  Pulling `E:` means pulling the backup drive with it. Also: step 17 of the reinstall plan only
  ever kept the SanDisk case out, and said to plug the Seagate back in. The plan's rule for `E:`
  was never to install or run anything from its flagged folders, not to disconnect it. An
  earlier message in Session 1 said otherwise and was wrong.
- **The 09-12 USB power-management fix almost certainly did not survive the rebuild.** It lived in
  the old install's power plan and registry (selective suspend off, disk spindown never,
  per-device values), and a fresh install resets those to defaults. If the Seagate stays
  attached long-term, re-apply it first, or the idle drop-outs from [[Machine drives]] come back.
  Not verified; needs a working shell.
- `E:` holds a Windows File History mirror of the whole old profile, including the known-bad
  AOMEI repack and the cracks lure. Recovery source of last resort only, after scanning.
  Detail in [[Security incident 2026-09-12]].
- Password rotation: keep working down `sites-to-rotate-PRIORITISED.txt`, banks first.
- Equifax credit ban still worth considering.
- Quarantine decision for the `E:` crack stash.

### Decisions Made

- None yet today.

### Notes Touched

- [[2026-09-15]]

### Profile Updates

- None.

## Session 2: GitHub push prep (time not captured; this session had no shell to read the clock)

### What Got Done

- Nathan is signing [[GitHub]] back in on the clean install and asked for the waiting vault
  commits to be pushed once he has.
- **12 commits are waiting, not 7.** Read straight from the vault's `.git`: local `main` is
  `5d6054c`, and the last pushed commit is `2572cc9` from 09-12, the one made just before the
  OAuth logins were revoked. "7" was the 09-13 count. It was carried forward without adding the
  five pre-wipe commits made on 09-15.
- **Nothing since the wipe has been committed.** Session 3 of [[2026-09-15]], this note, and the
  `VAULT-INDEX.md` path fix all sit uncommitted on top of `5d6054c`. In `my-agent`, `main` is
  level with `origin/main` (`2f5cbba`), but the 09-16 config repairs (`CLAUDE.md`,
  `.claude\settings.json`, `tool-configs\`) are uncommitted.
- **Neither Git nor `gh` is installed on the rebuild.** `winget` is present. The global git
  identity went with the old profile, so it needs setting again.
- **The shell is dead again.** Every PowerShell command in this session, including a bare
  string, returned exit 1 with no output. It's the same fault as Session 3 of [[2026-09-15]].
  The sandbox was left on, per the access-ramp rule.
- Handed Nathan the commands: install Git, set the identity, fetch (which triggers the sign-in),
  push the 12, then a checkpoint commit and push for both repos. See [[Vault backup]].
- **Pushed and verified, 12:36 AM** (the checkpoint commit's timestamp). Nathan ran the
  commands. In both repos the local `main` matches `origin/main`, which git only moves after a
  push succeeds: `the-beasts-brain` at `b7e4f45` ("Rebuild: restore verified, live paths moved
  to Fredy 2", on top of the 12), `the-beast-agent` at `fd8e579`. The backup is off-machine
  again for the first time since 09-12.
- Updated [[Vault backup]] for the rebuild: `Fredy 2` paths, the 09-12 cutoff and 09-16
  re-establishment, Git in place of `gh`, and the dead-shell caveat on automatic pushes.
  Updated the `vault-backup-auto-push` memory to say the standing authorization is live again.

### What's Still In Progress

- **The dead shell in Claude desktop sessions on this install** hasn't been diagnosed yet. Until
  it's fixed, every automatic checkpoint push under the [[Vault backup]] rule lands on Nathan.
- The [[Vault backup]] update and this entry were written after `b7e4f45`, so they need one more
  commit and push. The same goes for Session 1's later edits to this note, if they landed after
  that commit.

### Decisions Made

- None.

### Notes Touched

- [[Vault backup]]

### Profile Updates

- None.
