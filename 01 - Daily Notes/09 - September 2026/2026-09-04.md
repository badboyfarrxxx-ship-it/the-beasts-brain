---
status: active
project: personal
type: log
created: 2026-09-04
---

# Friday, September 4, 2026

## Index

- **tiny11 build staged:** set up a full tiny11builder environment on `H:`, picked the only clean source ISO of the five on `E:`, made the build fully offline, and wrote Nathan a step-by-step for installing the result on a second computer.

## Session 1, 3:03 PM: tiny11 Windows 11 image build

### What Got Done

- Nathan pointed at three files in Temp and asked for build instructions. **All three were scrambled**: the file named `autounattend.xml` held the GitHub funding file, `tiny11Coremaker.ps1` held the README, and `tiny11maker.ps1` held the real `autounattend.xml`. Not one was a PowerShell script, which is why the README made no sense to him. Whatever unpacked them into Temp shuffled the names.
- Extracted the intact zip from Downloads properly and set the build up at **`H:\tiny11builder\`**, not `C:`, because C: has only 16.9 GB free and the build needs 25 to 30 GB. Putting the folder on `H:` also makes the script's default scratch path correct with no parameter needed.
- Read all five Windows 11 ISOs in `E:\windows images-iso recovery\` by their volume descriptors rather than their filenames. Only `Win11_English_x64v1.iso` is a genuine unmodified Microsoft image (`CCCOMA_X64FRE_EN-US_DV9`, 2021-11-05). One repack wears the same genuine label but is dated a week before Windows 11 was announced, so it is the leaked 21996 pre-release. Another has no readable volume descriptor at all.
- Nathan is low on internet data and asked whether the build would use much. Read the whole script for network calls: exactly one, `oscdimg.exe`, 143,360 bytes. **Downloaded it into the build folder with his OK, so the build is now completely offline.**
- Wrote `TINY11 - FULL INSTRUCTIONS.txt` (build, Rufus, the other computer, and post-install data control), master copy in `H:\tiny11builder\`, working copy on the `D:` stick along with `ChromeSetup.exe`.
- Found Rufus already on disk at `E:\tools\rufus-3.18.exe`, so no download needed there either.

### What's Still In Progress

- **The build has not been run yet.** Nathan needs to mount the ISO and run `tiny11maker.ps1` from an elevated Windows PowerShell; it needs his UAC click and answers to two prompts (drive letter, image index).
- After the build, Rufus writes the ISO to `D:`, which erases the stick, so the instructions file and Chrome installer need copying back on afterwards.

### Decisions Made

- **Clean Microsoft ISO only, never a repack.** The pre-activated and "TPM bypassed" images already carry someone else's registry edits and activators; layering tiny11's DISM surgery on top makes an image where nothing can be attributed when it misbehaves, and this one is going onto a second machine he will log into things from.
- Chose the 21H2 clean ISO over downloading a current 25H2 one, because data is short. Accepted trade-off: the finished machine has four years of updates waiting, so the instructions tell him to keep it offline or set the connection as metered.
- `tiny11maker.ps1`, not `tiny11Coremaker.ps1`. Core strips WinSxS, Defender, Windows Update and WinRE with no way back, which is testbed-only territory.

### Notes Touched

- Created [[Building tiny11 images]] in `04 - Resources/`, the master note for this job.
- Updated [[Machine drives]]: **only three disks are attached now** (NVMe, the 4 TB Seagate, and a Lexar 28.9 GB flash drive). The Storage Space is gone from the system entirely, `G:` no longer exists, and `D:` is now the Lexar stick rather than a Storage Space partition, so the old "do not write to `D:`" warning was stale and is marked superseded. The dead "Generic STORAGE DEVICE" is also no longer attached, clearing two of that note's open items. Also recorded that `C:` is down to 16.9 GB free from ~34 GB.
- Updated the `04 - Resources` folder index for both.

### Profile Updates

- Nathan is currently rationing internet data. Worth checking before proposing anything that downloads.
