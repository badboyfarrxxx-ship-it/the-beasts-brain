---
status: active
project: personal
type: log
created: 2026-09-26
---

# Saturday, September 26, 2026

## Index

- **VLC test and a terminal rule:** found why VLC wouldn't start (a stale skins2 interface setting) and it now opens; added a boot-config rule to always name which terminal a command block is for.

## Session 1, 4:50 PM: VLC test and a terminal rule

### What Got Done
- Nathan downloaded and extracted the VLC 4 build zip. The files he sent were real VLC files (`libvlc.dll`, `libvlccore.dll`, AUTHORS, COPYING, NEWS) but no `vlc.exe`. Either the chat upload dropped the exe and folders, or Defender removed `vlc.exe`. Sent a check to find out.
- He pasted a PowerShell block into Git Bash, which failed. Added a rule to `CLAUDE.md` in `the-beast-agent`: name the shell above every command block and write it for that shell, PowerShell by default. Pushed on branch `claude/busy-keller-svqf6k` with a draft PR.

- The folder listing showed everything present (`vlc.exe`, `plugins`, and the rest), and Defender had removed nothing.
- Smart App Control is on (value 1), but Code Integrity logged no blocks.
- VLC's own log showed the real cause: it picked the skins2 interface (`skins2 error: no skins found : exiting`) because of an old `intf` line in `%APPDATA%\vlc\vlcrc`. Renamed the settings folder to `vlc-old-<date>` and VLC opened with its welcome page.

### What's Still In Progress
- Nathan to play a video, the last part of the test. Then retire [[Building VLC for Windows]] and start swipe to seek.

### Decisions Made
- Dropped the Smart App Control options (turning it off, or signing builds). It isn't what was stopping VLC, so the Surface keeps that protection.
- The terminal rule went into the boot config, not VAULT-INDEX, because "My Preferences for Working with AI" is Nathan-only to edit and always-on rules live in the boot config.

### Notes Touched
- `CLAUDE.md` in `the-beast-agent`; [[VLC build pipeline]], [[Building VLC for Windows]], [[Active Priorities]], [[01 - Daily Notes]] index.

### Profile Updates
-
